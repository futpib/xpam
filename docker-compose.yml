version: '3'
services:

  bot:
    build: ./bot
    environment:
    - PGHOST=postgres
    - PGUSER=postgres

  postgres:
    image: postgres:11
    volumes:
    - ./postgres/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
